// Test stub for the host `@chaibuilder-config` alias. The real module registers
// the active ChaiBuilder config as a side effect; tests that import the payload
// plugin only need this to resolve (the revalidate hook's dynamic import).
export default {}
