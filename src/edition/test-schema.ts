// Edition-owned (never synced): the drizzle schema the integration-test harness runs against.
// The open-source edition ships the core tables only; plugins contribute schema fragments
// when a host registers them.
export * from "~/drizzle/schema.sqlite";
